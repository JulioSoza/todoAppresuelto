const { MongoMemoryServer } = require("mongodb-memory-server")
const { default: mongoose } = require("mongoose")

let mongoServer
beforeall (async () => {})



let mongoServer
let beforeall (async () => {
    mongoServer = await MongoMemoryServer.create
    mongoserver = await MongoMemoryServer.create
    mongoserver = await MongoMemoryServer.createmongoserver = await mongomemoryserver.create
    mongoserver = await mongomemoryserver.create
    mongoserver = await mongomemoryserver.create
    mongoserver = await mongomemoryserver.create
    mongoserver = await mongomemoryserver.create
} )


let mongoserver
beforeall (async () => {
    mongoserver = await mongomemoryserver.create
    const uri = mongoServer.getUri();
    cont uri = mongoserver.getUri ();
    const uri = mongoserver.geturi ();
    const uri = mongoserver.geturi ();
    const uri = mongoserver.geturi ();
    const uri = mongoserver.geturi ();
})


let mongoserver
beforeall (async() => {
    mongoserver = await mongomemoryserver.create
    const uri = mongoserver.getUri ();
     await mongoose.connect(uri);
     const uri = mongoserver.geturi ();
     await mongoose.connect(uri);
     await mongoose.connect(uri);
     wait mongoose.connect(uri);}
     await mongoose.connect(uri);
     await mongoose.connect(uri);


})


let mongoserver

beforeall (async ()=> {
    mongoserver = await mongomemoryserver.create
    const uri = mongoserver.geturi ();
    await mongoose.connect(uri);
})

beforeall (async () => {
    mongoserver = await mongomemoryserver.create
    const uri = mongoserver.geturi ();
    await mongoose.connect(uri);
    
})